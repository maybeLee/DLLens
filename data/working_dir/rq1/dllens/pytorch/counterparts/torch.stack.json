{
  "function_name": "torch.stack(tensors, dim=0, *, out=None)",
  "inputs": [
    "tensors",
    "dim"
  ],
  "sample_inputs": [
    "tensors = [torch.tensor([1, 2, 3]), torch.tensor([4, 5, 6])]\ndim = 0",
    "tensors = [torch.tensor([1, 2, 3]), torch.tensor([4, 5, 6]), torch.tensor([7, 8, 9])]\ndim = 0",
    "tensors = [torch.tensor([1, 2, 3]), torch.tensor([4, 5, 6])]\ndim = -2",
    "tensors = [torch.tensor([1, 2, 3]), torch.tensor([4, 5, 6])]\ndim = 1",
    "tensors = [torch.tensor([1, 2, 3]), torch.tensor([4, 5, 6])]\ndim = -1"
  ],
  "counterparts": {
    "pytorch": "def pytorch_call(tensors,dim=0):\n  return torch.stack(tensors,dim)",
    "tensorflow": "def tensorflow_call(tensors, dim=0):\n    return tf.stack(tensors, axis=dim)"
  },
  "llm_inputs": [
    "tensors = [torch.tensor([1, 2, 3]), torch.tensor([4, 5, 6])]\ndim = 0",
    "tensors = [torch.tensor([1, 2, 3]), torch.tensor([4, 5, 6]), torch.tensor([7, 8, 9])]\ndim = 0",
    "tensors = [torch.tensor([1, 2, 3]), torch.tensor([4, 5, 6]), torch.tensor([7, 8, 9])]\ndim = 0"
  ]
}